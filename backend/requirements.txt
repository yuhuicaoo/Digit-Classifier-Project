fastapi==0.95.0              # FastAPI framework
uvicorn==0.22.0              # ASGI server for FastAPI
pillow==9.4.0                # For image processing (PIL)
torch==1.13.1                # PyTorch for the CNN model
torchvision==0.14.1          # For image transforms (e.g., Resize, PILToTensor)
pydantic==1.10.5             # Data validation (used by FastAPI)
aiofiles==0.8.0              # For handling file uploads asynchronously
matplotlib==3.7.1            # Matplotlib for plotting
